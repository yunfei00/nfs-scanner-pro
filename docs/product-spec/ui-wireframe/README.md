# UI Wireframe 总览

从 Release 008 开始，NFS Scanner Pro 不再直接生成高保真，而是先固定线框。

核心原则：

1. PCB 是主角，默认占据最大面积。
2. 左侧导航默认仅显示图标，鼠标悬停后展开文字。
3. 右侧参数区使用 Dock，可固定、自动隐藏、关闭。
4. 日志、频谱、统计默认隐藏，通过“视图”菜单打开。
5. 项目不作为左侧一级页面，项目的新建、打开、保存放在“文件”菜单。
6. 设置和帮助放在顶级菜单，不进入左侧业务导航。
